# Windows release output

The verified portable build will be stored here as:

`WebernCompositionalModel-Windows-x64.exe`

The self-extracting launcher will contain the Windows Max runtime, bach
externals, engine, profiles and audition voice. It extracts into a private
temporary directory, runs the application, then removes the temporary copy.
