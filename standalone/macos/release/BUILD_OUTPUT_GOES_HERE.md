# macOS release output

The verified portable build will be stored here as:

`WebernCompositionalModel-macOS-universal.zip`

The ZIP will contain the single visible
`WebernCompositionalModel.app` bundle with the Max runtime, bach externals,
engine, profiles and audition voice encapsulated inside it.
